# Counter

A simple NIP-5D napplet that stores a counter value through NAP-STORAGE.

```bash
pnpm --filter counter build
pnpm --filter counter type-check
pnpm --filter counter test:conformance
```

The stlstr dev server serves the built artifact at `/napplets.dev/counter/index.html`.
