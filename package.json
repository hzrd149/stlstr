{
  "name": "stlstr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A pnpm monorepo: the stlstr Vite host app and a collection of NIP-5D napplets.",
  "scripts": {
    "new": "node scripts/new-napplet.mjs",
    "dev": "node scripts/dev.mjs",
    "prepare": "pnpm build",
    "build": "VITE_DEV_PRIVKEY_HEX=${VITE_DEV_PRIVKEY_HEX:-00000000000000000000000000000000000000000000000000000000deadbeef} turbo run build --filter=...{./napplets/*}",
    "build:app": "turbo run build --filter=@apps/stlstr",
    "verify": "pnpm test && VITE_DEV_PRIVKEY_HEX=${VITE_DEV_PRIVKEY_HEX:-00000000000000000000000000000000000000000000000000000000deadbeef} turbo run type-check build --filter={./napplets/*} --filter={./lib/*}",
    "test": "pnpm test:scripts && turbo run test --filter={./napplets/*} --filter={./lib/*}",
    "test:scripts": "node --test scripts/lib/*.test.mjs",
    "test:browser": "node scripts/test-browser.mjs",
    "type-check": "turbo run type-check --filter={./napplets/*} --filter={./lib/*}",
    "discover": "napplet discover --all",
    "test:conformance": "pnpm build && napplet conformance --all",
    "login": "napplet keys store --name stlstr --prompt-sec && napplet keys use --name stlstr --config .napplet/config.dev.json",
    "logout": "napplet keys delete --name stlstr",
    "keys": "napplet keys",
    "deploy": "node scripts/check-dev-target.mjs && pnpm build && napplet deploy --all --config .napplet/config.dev.json",
    "deploy:dry": "node scripts/check-dev-target.mjs && pnpm build && napplet deploy --all --dry-run --config .napplet/config.dev.json",
    "deploy:prod": "pnpm build && napplet deploy --all --config .napplet/config.json",
    "deploy:prod:dry": "pnpm build && napplet deploy --all --dry-run --config .napplet/config.json",
    "debug": "napplet debug --all",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@11.16.0",
  "devDependencies": {
    "@kehto/cli": "^0.2.16",
    "@napplelets/napplet-cli": "workspace:*",
    "@napplet/conformance-cli": "^0.2.15",
    "nostr-tools": "^2.24.1",
    "prettier": "^3.9.5",
    "prettier-plugin-svelte": "^3.5.2",
    "puppeteer": "^24.31.0",
    "turbo": "^2.10.5",
    "ws": "^8.21.1"
  },
  "license": "MIT"
}
